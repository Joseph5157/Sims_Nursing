# NPM Configuration
legacy-peer-deps=false
audit=true
fund=false

# The "production" config warning appears because NODE_ENV=production is set
# in Railway, but this is not an error—just a deprecation notice.
# The build process explicitly uses --omit=dev, so we're using modern practices.
