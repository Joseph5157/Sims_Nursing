**SectionHeader** — uppercase muted label above every list or section. Pairs a title with an optional right-aligned action.

```jsx
<SectionHeader title="Upcoming duties" action={<a href="#">See all →</a>} />
```
