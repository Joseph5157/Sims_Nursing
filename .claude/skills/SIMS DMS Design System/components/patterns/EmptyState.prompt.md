**EmptyState** — centered placeholder for any empty list (RULE 7: never show a blank screen).

```jsx
<EmptyState emoji="📅" title="No duty today"
  subtitle="Check your upcoming slots below"
  action={<Button variant="secondary">Pick slots</Button>} />
```
